# Problem 2: CSV Column Mean

## Specification
Read the weather data in `input/weather.csv` and compute the mean of the `temp_c` column across all 365 rows.

## Output
Save your output to: `problems/02-csv-column-mean/output.txt`

The file should contain a single line: the mean temperature rounded to exactly 2 decimal places. Example format: `3.47`
